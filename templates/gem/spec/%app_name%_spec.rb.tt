require "spec_helper"

describe <%= Thor::Util.camel_case(app_name) %> do
  it "has a version number" do
    expect(<%= Thor::Util.camel_case(app_name) %>::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
